apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanderhesselinknl.fullname" . }}
  labels:
    {{- include "sanderhesselinknl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sanderhesselinknl.selectorLabels" . | nindent 4 }}
