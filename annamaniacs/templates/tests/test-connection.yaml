apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "annamaniacs.fullname" . }}-test-connection"
  labels:
    {{- include "annamaniacs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "annamaniacs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
