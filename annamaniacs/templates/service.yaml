apiVersion: v1
kind: Service
metadata:
  name: {{ include "annamaniacs.fullname" . }}
  labels:
    {{- include "annamaniacs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "annamaniacs.selectorLabels" . | nindent 4 }}
