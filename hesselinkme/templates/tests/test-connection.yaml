apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hesselinkme.fullname" . }}-test-connection"
  labels:
    {{- include "hesselinkme.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hesselinkme.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
