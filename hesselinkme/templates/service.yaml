apiVersion: v1
kind: Service
metadata:
  name: {{ include "hesselinkme.fullname" . }}
  labels:
    {{- include "hesselinkme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hesselinkme.selectorLabels" . | nindent 4 }}
