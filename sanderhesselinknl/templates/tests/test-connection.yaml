apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sanderhesselinknl.fullname" . }}-test-connection"
  labels:
    {{- include "sanderhesselinknl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sanderhesselinknl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
